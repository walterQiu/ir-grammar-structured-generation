experiment_name: eae_ir

dataset:
  name: rams
  data_dir: datasets/RAMS/data
  split: train
  max_samples: 100

model:
  name: ir_pipeline

evaluation:
  validators: [strict]
  metrics: [strict_rates]
