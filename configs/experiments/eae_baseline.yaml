experiment_name: eae_baseline

dataset:
  name: rams
  data_dir: datasets/RAMS/data
  split: train
  max_samples: 100

model:
  name: baseline

evaluation:
  validators: [strict]
  metrics: [strict_rates]
