experiment_name: eae_baseline

dataset:
  name: rams
  data_dir: datasets/RAMS/data
  split: train
  max_samples: 100

model:
  name: baseline
  backend: mock
  llm_name: gemini-2.5-flash-lite
  api_key_env: GEMINI_API_KEY
  dotenv_path: dotenv/.env
  temperature: 0.0

evaluation:
  validators: [strict]
  metrics: [strict_rates]
